# Placeholder. Swap for sealed-secrets / External Secrets Operator in real envs.
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-db
  labels:
    app.kubernetes.io/name: {{name}}
type: Opaque
stringData:
  connectionString: "Host=postgres;Database=app;Username=dev;Password=dev"
