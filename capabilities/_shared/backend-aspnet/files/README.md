# {{Name}}

Generated by [`dev-start`](https://github.com/stefan-chiforiuc/dev-start).

## Run it

```sh
just bootstrap    # compose up + migrate + seed + test
```

Then open the dashboard at <http://localhost:4000>.

## Day-to-day

```sh
just build
just test
just db-migrate-add <Name>
dev-start add <capability>      # add cache, s3, queue, ...
dev-start doctor                # diagnose drift and missing services
```

## Opinions

This project follows the [paved road](https://github.com/stefan-chiforiuc/dev-start/blob/main/docs/paved-road.md).
Disagreements live in [`docs/adr/`](./docs/adr).
