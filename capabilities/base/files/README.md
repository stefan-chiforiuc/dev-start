# {{Name}}

Generated by [`dev-start-dotnet`](https://github.com/stefan-chiforiuc/dev-start-dotnet).

## Run it

```sh
just bootstrap    # compose up + migrate + seed + test
```

Then open the dashboard at <http://localhost:4000>.

## Day-to-day

```sh
just build
just test
just db-migrate-add <Name>
dev-start-dotnet add <capability>      # add cache, s3, queue, ...
dev-start-dotnet doctor                # diagnose drift and missing services
```

## Opinions

This project follows the [paved road](https://github.com/stefan-chiforiuc/dev-start-dotnet/blob/main/docs/paved-road.md).
Disagreements live in [`docs/adr/`](./docs/adr).
