{
  "name": "{{name}}",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "dev": "pnpm --filter api dev",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "5.6.3",
    "vitest": "2.1.2",
    "@types/node": "22.7.5"
  }
}
